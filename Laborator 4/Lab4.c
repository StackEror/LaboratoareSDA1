#include "BST.h"

int main()
{
    do_job();
    return 0;
}


// for(int i = 0; i < total_number_of_nodes(root); i++)
    // {
    //     free(tree_nodes[i]);
    // }
    

    // for(int h = 0; h < total_number_of_nodes(root); h++)
    //     {
    //         free(tree_nodes[h]);
    //     }
    // if(tree_nodes)
    // {
    //     for(int h = 0; h < total_number_of_nodes(root); h++)
    //     {
    //         printf("lista %d ",tree_nodes[h]->key);
    //     }
    // }
    // else
    //     printf("Lista nu exista\n");

    // printf("\n");



